#include<iostream>
using namespace std;

int main(){
    int y;

    int num1, num2; //set up two variables

    cout<<"A couple of nums if u could "
    cin >> num1 >> num2;

    if (num2 == 0) {
        cout << "division by "<<num1<<"by"<<num2<<"nope!" << endl;
        
    }
    if (float num3= num1 / num2; 

        cout << "in order "<< endl;
    }
    cout << "in order "<< endl;

    num1 == 13 || num2 = 13) {
        cout << "bad luck" << endl;
    }
        
    
